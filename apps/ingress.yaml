---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo
  namespace: apps
spec:
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: "/echo"
        backend:
          service:
            name: echo
            port:
              number: 80
